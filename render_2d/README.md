 

## 第一次
- main
    - canvas 屏幕（窗口）上的像素，以左上角为原点
        - pixels  数组长度：窗口宽 * 窗口高
            - 坐标（x, y）对应的像素值：y * 窗口宽 + x
            - drawPoint
    - color
    - vector
    - render
        - update
        - clear
        - draw
        - show

